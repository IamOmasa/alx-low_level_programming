#include <stdlib.h>
#include <stdio.h>

/**
 * main - program that print its name
 * @argc: hold number of arguments passed
 * @argv: array pointer that holds the argument passed
 *
 * Return: Always 0 (Success)
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	int i;

	printf("%s\n", argv[i]);

	return (0);
}
